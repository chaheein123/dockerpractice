name: Fibonacci App Prod Deployment
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# env:
#   GH_TOKEN: ${{ secrets.BAZEL_IO_TOKEN }}


jobs:
  # Test
  # test-client:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3  # Tells the robot to "git clone" your code
      
  #     - name: Build the Test Image
  #       run: docker build -t ian/react-test -f ./client/Dockerfile.dev ./client
        
  #     - name: Run Tests
  #       run: docker run -e CI=true ian/react-test npm test

  build:
    # needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build the Docker images
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build Docker images
        run: |
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/dp-aws-nginx:${{ github.sha }} -f ./nginx/Dockerfile ./nginx
        shell: bash
      # - uses: actions/checkout@v3
      #   name: Build the Docker images
      #   run: |
      #   curl -LO "https://github.com/bazelbuild/bazelisk/releases/download/v1.19.0/bazelisk-linux-amd64"
      #   chmod +x bazelisk-linux-amd64
      #   shell: bash
      



  # Push the images to the Docker Hub


  # Use AWS CLI/CodeDeploy to deploy the apps (Enterprise standard) OR Give your EC2 an IAM Role that allows it to talk to SSM (aws ssm send-command --document-name "AWS-RunShellScript" --targets "InstanceIds=i-12345" --parameters 'commands=["cd /app && docker-compose pull && docker-compose up -d"]')

  # AWS_ACCESS_KEY_ID

  # AWS_SECRET_ACCESS_KEY

  # AWS_REGION (e.g., us-east-1)

  # DOCKER_HUB_USERNAME / PASSWORD